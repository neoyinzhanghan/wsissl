from setuptools import setup, find_packages

setup(name='wsissl',
      description='Trainings and deployments of heavy weight self-supervised learning (SSL) models from existing codebases for applications to whole slide images (WSI).',
      version='0.1',
      packages=find_packages(),
      install_requires=[])
