from setuptools import setup, find_packages

setup(name='heavily',
      description='Trainings and deployments of heavy weight self-supervised learning (SSL) models from existing codebases.',
      version='0.1',
      packages=find_packages(),
      install_requires=[])
